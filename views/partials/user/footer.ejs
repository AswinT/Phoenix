<!-- Footer Section -->
<footer class="elegant-footer">
  <div class="container-fluid">
    <!-- Main Footer Content -->
    <div class="footer-main">
      <div class="row">
        <!-- Brand Section -->
        <div class="col-lg-4 col-md-6 mb-4">
          <div class="footer-brand">
            <div class="brand-logo-footer">
              <img
                src="/assets/phoenix-logo.png"
                alt="Phoenix Logo"
                class="footer-logo"
              />
              <h3 class="brand-name">Phoenix</h3>
            </div>
            <p class="brand-description">
              Elevating your audio experience with premium headphones designed
              for audiophiles who demand excellence in every note.
            </p>
            <div class="social-links">
              <a href="#" class="social-link" title="Facebook">
                <i class="ri-facebook-fill"></i>
              </a>
              <a href="#" class="social-link" title="Twitter">
                <i class="ri-twitter-fill"></i>
              </a>
              <a href="#" class="social-link" title="Instagram">
                <i class="ri-instagram-line"></i>
              </a>
              <a href="#" class="social-link" title="YouTube">
                <i class="ri-youtube-fill"></i>
              </a>
              <a href="#" class="social-link" title="LinkedIn">
                <i class="ri-linkedin-fill"></i>
              </a>
            </div>
          </div>
        </div>

        <!-- Quick Links -->
        <div class="col-lg-2 col-md-6 mb-4">
          <div class="footer-section">
            <h4 class="footer-title">Quick Links</h4>
            <ul class="footer-links">
              <li><a href="/">Home</a></li>
              <li><a href="/shopPage">Shop</a></li>
              <li><a href="/about">About Us</a></li>
              <li><a href="/contact">Contact</a></li>
              <li><a href="/blog">Blog</a></li>
            </ul>
          </div>
        </div>

        <!-- Categories -->
        <div class="col-lg-2 col-md-6 mb-4">
          <div class="footer-section">
            <h4 class="footer-title">Categories</h4>
            <ul class="footer-links">
              <li><a href="/shopPage?category=wireless">Wireless</a></li>
              <li><a href="/shopPage?category=wired">Wired</a></li>
              <li><a href="/shopPage?category=gaming">Gaming</a></li>
              <li><a href="/shopPage?category=studio">Studio</a></li>
              <li><a href="/shopPage?category=sport">Sports</a></li>
            </ul>
          </div>
        </div>

        <!-- Customer Service -->
        <div class="col-lg-2 col-md-6 mb-4">
          <div class="footer-section">
            <h4 class="footer-title">Support</h4>
            <ul class="footer-links">
              <li><a href="/help">Help Center</a></li>
              <li><a href="/shipping">Shipping Info</a></li>
              <li><a href="/returns">Returns</a></li>
              <li><a href="/warranty">Warranty</a></li>
              <li><a href="/track-order">Track Order</a></li>
            </ul>
          </div>
        </div>

        <!-- Contact Info -->
        <div class="col-lg-2 col-md-6 mb-4">
          <div class="footer-section">
            <h4 class="footer-title">Contact</h4>
            <div class="contact-info">
              <div class="contact-item">
                <i class="ri-phone-line"></i>
                <span>+1 (555) 123-4567</span>
              </div>
              <div class="contact-item">
                <i class="ri-mail-line"></i>
                <span>info@phoenix.com</span>
              </div>
              <div class="contact-item">
                <i class="ri-map-pin-line"></i>
                <span>123 Audio Street<br />Music City, MC 12345</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Newsletter Section -->
    <div class="newsletter-section">
      <div class="row align-items-center">
        <div class="col-lg-6 mb-3 mb-lg-0">
          <div class="newsletter-content">
            <h4 class="newsletter-title">Stay in the Loop</h4>
            <p class="newsletter-subtitle">
              Get the latest updates on new products, exclusive offers, and
              audio insights.
            </p>
          </div>
        </div>
        <div class="col-lg-6">
          <form class="newsletter-form" id="newsletterForm">
            <div class="input-group">
              <input
                type="email"
                class="form-control newsletter-input"
                placeholder="Enter your email address"
                required
              />
              <button class="btn newsletter-btn" type="submit">
                <span>Subscribe</span>
                <i class="ri-arrow-right-line"></i>
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- Footer Bottom -->
    <div class="footer-bottom">
      <div class="row align-items-center">
        <div class="col-lg-6 mb-2 mb-lg-0">
          <div class="copyright">
            <p>&copy; 2025 Phoenix Audio. All rights reserved.</p>
          </div>
        </div>
        <div class="col-lg-6">
          <div class="footer-legal">
            <a href="/privacy-policy">Privacy Policy</a>
            <a href="/terms-of-service">Terms of Service</a>
            <a href="/cookies">Cookies</a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Floating Background Elements -->
  <div class="footer-bg-elements">
    <div class="bg-circle bg-circle-1"></div>
    <div class="bg-circle bg-circle-2"></div>
    <div class="bg-circle bg-circle-3"></div>
  </div>
</footer>

<style>
  /* Root variables for consistent color scheme */
  :root {
    --primary-color: #1b3c53; /* Deep blue-gray (primary) */
    --secondary-color: #456882; /* Medium blue-gray (secondary) */
    --accent-beige: #d2c1b6; /* Light beige (secondary) */
    --accent-cream: #f9f3ef; /* Off-white/cream (secondary) */
    --supporting-white: #ffffff; /* Pure white for contrast */
    --text-dark: #2d3748; /* Dark text color */
    --error-red: #d63031; /* Error red */
    --success-green: #00b894; /* Success green */

    /* Gradients */
    --gradient-primary: linear-gradient(
      135deg,
      var(--primary-color),
      var(--secondary-color)
    );
    --gradient-secondary: linear-gradient(
      135deg,
      var(--secondary-color),
      var(--accent-beige)
    );
    --gradient-footer: linear-gradient(
      135deg,
      var(--primary-color) 0%,
      var(--secondary-color) 50%,
      var(--accent-beige) 100%
    );

    /* Shadows */
    --shadow-footer: 0 -4px 25px rgba(27, 60, 83, 0.2);

    /* Typography */
    --font-heading: "Audiowide", cursive;
    --font-body: "Raleway", sans-serif;
    --font-feature: "Montserrat", sans-serif;
  }

  /* Main Footer Container - Full Width, Compact */
  .elegant-footer {
    background: var(--gradient-footer);
    color: var(--supporting-white);
    font-family: var(--font-body);
    position: relative;
    overflow: hidden;
    box-shadow: var(--shadow-footer);
    margin-top: auto;
    width: 100%;
  }

  .elegant-footer::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="20" cy="20" r="1" fill="rgba(255,255,255,0.1)"/><circle cx="60" cy="60" r="1" fill="rgba(255,255,255,0.08)"/><circle cx="80" cy="30" r="1" fill="rgba(255,255,255,0.06)"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
    opacity: 0.3;
    pointer-events: none;
  }

  /* Full Width Container - Compact */
  .elegant-footer .container-fluid {
    position: relative;
    z-index: 2;
    padding-left: 2rem;
    padding-right: 2rem;
    max-width: 100%;
    width: 100%;
  }

  /* Footer Main Content - Reduced Height */
  .footer-main {
    padding: 3rem 0 1.5rem;
  }

  .footer-main .row {
    margin-left: 0;
    margin-right: 0;
    width: 100%;
  }

  .footer-main .col-lg-4,
  .footer-main .col-lg-2,
  .footer-main .col-md-6 {
    padding-left: 1.5rem;
    padding-right: 1.5rem;
  }

  /* Brand Section - Compact */
  .footer-brand {
    margin-bottom: 1rem;
  }

  .brand-logo-footer {
    display: flex;
    align-items: center;
    margin-bottom: 1rem;
    gap: 1rem;
  }

  .footer-logo {
    height: 50px;
    width: auto;
    filter: drop-shadow(0 4px 12px rgba(249, 243, 239, 0.3));
    transition: all 0.3s ease;
  }

  .footer-logo:hover {
    filter: drop-shadow(0 6px 18px rgba(249, 243, 239, 0.5));
    transform: scale(1.05);
  }

  .brand-name {
    font-family: var(--font-heading);
    font-size: 2.2rem;
    margin: 0;
    background: linear-gradient(
      135deg,
      var(--accent-cream),
      var(--accent-beige)
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
  }

  .brand-description {
    font-size: 1rem;
    line-height: 1.6;
    color: rgba(249, 243, 239, 0.9);
    margin-bottom: 1.5rem;
    max-width: 350px;
  }

  /* Social Links - Compact */
  .social-links {
    display: flex;
    gap: 1rem;
  }

  .social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 45px;
    height: 45px;
    background: rgba(249, 243, 239, 0.1);
    border: 2px solid rgba(249, 243, 239, 0.2);
    border-radius: 50%;
    color: var(--accent-cream);
    text-decoration: none;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
  }

  .social-link:hover {
    background: var(--accent-cream);
    color: var(--primary-color);
    transform: translateY(-3px) scale(1.1);
    box-shadow: 0 8px 20px rgba(249, 243, 239, 0.3);
  }

  .social-link i {
    font-size: 1.2rem;
  }

  /* Footer Sections - Compact */
  .footer-section {
    margin-bottom: 1rem;
  }

  .footer-title {
    font-family: var(--font-feature);
    font-size: 1.2rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: var(--accent-cream);
    position: relative;
    padding-bottom: 0.25rem;
  }

  .footer-title::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 40px;
    height: 2px;
    background: var(--gradient-secondary);
    border-radius: 2px;
  }

  /* Footer Links - Compact */
  .footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer-links li {
    margin-bottom: 0.5rem;
  }

  .footer-links a {
    color: rgba(249, 243, 239, 0.8);
    text-decoration: none;
    font-size: 0.95rem;
    transition: all 0.3s ease;
    display: inline-block;
    position: relative;
    padding: 0.1rem 0;
  }

  .footer-links a::before {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--accent-cream);
    transition: width 0.3s ease;
  }

  .footer-links a:hover {
    color: var(--accent-cream);
    transform: translateX(6px);
  }

  .footer-links a:hover::before {
    width: 100%;
  }

  /* Contact Info - Compact */
  .contact-info {
    margin-top: 0.5rem;
  }

  .contact-item {
    display: flex;
    align-items: flex-start;
    margin-bottom: 0.75rem;
    gap: 0.75rem;
  }

  .contact-item i {
    color: var(--accent-beige);
    font-size: 1.1rem;
    margin-top: 0.1rem;
    flex-shrink: 0;
  }

  .contact-item span {
    color: rgba(249, 243, 239, 0.9);
    font-size: 0.9rem;
    line-height: 1.4;
  }

  /* Newsletter Section - Compact */
  .newsletter-section {
    padding: 2rem 0;
    border-top: 1px solid rgba(249, 243, 239, 0.2);
    border-bottom: 1px solid rgba(249, 243, 239, 0.2);
    background: rgba(249, 243, 239, 0.05);
    backdrop-filter: blur(10px);
    margin-left: -2rem;
    margin-right: -2rem;
    padding-left: 2rem;
    padding-right: 2rem;
  }

  .newsletter-title {
    font-family: var(--font-heading);
    font-size: 1.6rem;
    margin-bottom: 0.5rem;
    color: var(--accent-cream);
  }

  .newsletter-subtitle {
    color: rgba(249, 243, 239, 0.8);
    margin-bottom: 0;
    font-size: 1rem;
    line-height: 1.5;
  }

  .newsletter-form .input-group {
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 15px rgba(27, 60, 83, 0.2);
    max-width: 450px;
    margin-left: auto;
  }

  .newsletter-input {
    background: rgba(249, 243, 239, 0.95);
    border: none;
    color: var(--text-dark);
    font-family: var(--font-body);
    padding: 1rem 1.5rem;
    font-size: 1rem;
    border-radius: 12px 0 0 12px;
  }

  .newsletter-input::placeholder {
    color: rgba(69, 104, 130, 0.7);
  }

  .newsletter-input:focus {
    background: var(--supporting-white);
    box-shadow: none;
    border-color: transparent;
  }

  .newsletter-btn {
    background: var(--gradient-primary);
    border: none;
    color: var(--supporting-white);
    font-family: var(--font-feature);
    font-weight: 600;
    padding: 1rem 2rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: all 0.3s ease;
    border-radius: 0 12px 12px 0;
  }

  .newsletter-btn:hover {
    background: var(--gradient-secondary);
    transform: translateX(2px);
    box-shadow: 0 4px 15px rgba(69, 104, 130, 0.3);
  }

  .newsletter-btn i {
    transition: transform 0.3s ease;
  }

  .newsletter-btn:hover i {
    transform: translateX(3px);
  }

  /* Footer Bottom - Compact */
  .footer-bottom {
    padding: 1.5rem 0;
    background: rgba(27, 60, 83, 0.2);
    backdrop-filter: blur(15px);
    margin-left: -2rem;
    margin-right: -2rem;
    padding-left: 2rem;
    padding-right: 2rem;
  }

  .copyright p {
    margin: 0;
    color: rgba(249, 243, 239, 0.8);
    font-size: 0.9rem;
  }

  .footer-legal {
    display: flex;
    justify-content: flex-end;
    gap: 2rem;
  }

  .footer-legal a {
    color: rgba(249, 243, 239, 0.8);
    text-decoration: none;
    font-size: 0.9rem;
    transition: all 0.3s ease;
    padding: 0.25rem;
  }

  .footer-legal a:hover {
    color: var(--accent-cream);
  }

  /* Background Elements - Adjusted for Compact Design */
  .footer-bg-elements {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
    z-index: 1;
    width: 100%;
    height: 100%;
  }

  .bg-circle {
    position: absolute;
    border-radius: 50%;
    background: rgba(249, 243, 239, 0.1);
    animation: float 6s ease-in-out infinite;
  }

  .bg-circle-1 {
    width: 80px;
    height: 80px;
    top: 15%;
    right: 10%;
    animation-delay: 0s;
  }

  .bg-circle-2 {
    width: 120px;
    height: 120px;
    bottom: 25%;
    left: 8%;
    animation-delay: -2s;
  }

  .bg-circle-3 {
    width: 60px;
    height: 60px;
    top: 60%;
    right: 5%;
    animation-delay: -4s;
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0px) rotate(0deg);
      opacity: 0.3;
    }
    50% {
      transform: translateY(-15px) rotate(180deg);
      opacity: 0.6;
    }
  }

  /* Responsive Design - Compact */
  @media (max-width: 1199.98px) {
    .elegant-footer .container-fluid {
      padding-left: 1.5rem;
      padding-right: 1.5rem;
    }

    .newsletter-section,
    .footer-bottom {
      margin-left: -1.5rem;
      margin-right: -1.5rem;
      padding-left: 1.5rem;
      padding-right: 1.5rem;
    }
  }

  @media (max-width: 991.98px) {
    .footer-main {
      padding: 2.5rem 0 1rem;
    }

    .brand-logo-footer {
      justify-content: center;
      text-align: center;
    }

    .brand-description {
      text-align: center;
      max-width: 100%;
    }

    .social-links {
      justify-content: center;
      gap: 1rem;
    }

    .social-link {
      min-width: 44px;
      min-height: 44px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
    }

    .footer-section {
      text-align: center;
    }

    .footer-links a {
      min-height: 44px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.5rem 1rem;
      border-radius: 4px;
      transition: all 0.3s ease;
    }

    .footer-title::after {
      left: 50%;
      transform: translateX(-50%);
    }

    .contact-item {
      justify-content: center;
    }

    .newsletter-section {
      text-align: center;
      padding: 1.5rem 0;
    }

    .newsletter-form {
      max-width: 400px;
      margin: 0 auto;
    }

    .newsletter-input {
      min-height: 44px;
      font-size: 1rem;
    }

    .newsletter-btn {
      min-height: 44px;
      min-width: 44px;
      font-size: 1.1rem;
    }

    .footer-legal {
      justify-content: center;
      flex-wrap: wrap;
      gap: 1.5rem;
    }

    .elegant-footer .container-fluid {
      padding-left: 1rem;
      padding-right: 1rem;
    }

    .newsletter-section,
    .footer-bottom {
      margin-left: -1rem;
      margin-right: -1rem;
      padding-left: 1rem;
      padding-right: 1rem;
    }
  }

  @media (max-width: 767.98px) {
    .footer-main {
      padding: 2rem 0 1rem;
    }

    .brand-name {
      font-size: 1.8rem;
    }

    .footer-logo {
      height: 40px;
    }

    .newsletter-form .input-group {
      flex-direction: column;
    }

    .newsletter-input,
    .newsletter-btn {
      border-radius: 10px;
      margin-bottom: 0.5rem;
    }

    .newsletter-btn {
      justify-content: center;
    }

    .social-links {
      gap: 0.75rem;
    }

    .social-link {
      width: 40px;
      height: 40px;
    }

    .footer-main .col-lg-4,
    .footer-main .col-lg-2,
    .footer-main .col-md-6 {
      padding-left: 1rem;
      padding-right: 1rem;
    }

    .newsletter-section {
      padding: 1.25rem 0;
    }
  }

  @media (max-width: 575.98px) {
    .footer-main {
      padding: 1.5rem 0 0.5rem;
    }

    .brand-name {
      font-size: 1.8rem;
    }

    .brand-description {
      font-size: 0.9rem;
      line-height: 1.6;
    }

    .social-links {
      gap: 0.75rem;
    }

    .social-link {
      min-width: 40px;
      min-height: 40px;
      font-size: 1.1rem;
    }

    .footer-links a {
      min-height: 40px;
      font-size: 0.9rem;
      padding: 0.4rem 0.8rem;
    }

    .newsletter-section {
      padding: 1rem 0;
    }

    .newsletter-title {
      font-size: 1.4rem;
    }

    .newsletter-input {
      min-height: 40px;
      font-size: 0.9rem;
    }

    .newsletter-btn {
      min-height: 40px;
      min-width: 40px;
      font-size: 1rem;
    }

    .footer-legal {
      font-size: 0.8rem;
      gap: 1rem;
    }

    .scroll-to-top {
      width: 45px;
      height: 45px;
      bottom: 20px;
      right: 20px;
    }

    .footer-bottom {
      padding: 1rem 0;
      text-align: center;
    }

    .copyright,
    .footer-legal {
      text-align: center;
      justify-content: center;
    }

    .elegant-footer .container-fluid {
      padding-left: 0.75rem;
      padding-right: 0.75rem;
    }

    .newsletter-section,
    .footer-bottom {
      margin-left: -0.75rem;
      margin-right: -0.75rem;
      padding-left: 0.75rem;
      padding-right: 0.75rem;
    }

    .footer-main .col-lg-4,
    .footer-main .col-lg-2,
    .footer-main .col-md-6 {
      padding-left: 0.75rem;
      padding-right: 0.75rem;
      margin-bottom: 1.5rem;
    }
  }

  /* Scroll to top button integration */
  .scroll-to-top {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    width: 50px;
    height: 50px;
    background: var(--gradient-primary);
    border: none;
    border-radius: 50%;
    color: var(--supporting-white);
    font-size: 1.2rem;
    cursor: pointer;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    z-index: 1000;
    box-shadow: 0 4px 15px rgba(27, 60, 83, 0.3);
  }

  .scroll-to-top.visible {
    opacity: 1;
    visibility: visible;
  }

  .scroll-to-top:hover {
    transform: translateY(-3px) scale(1.1);
    box-shadow: 0 8px 25px rgba(27, 60, 83, 0.4);
  }
</style>

<!-- Scroll to Top Button -->
<button class="scroll-to-top" id="scrollToTop" title="Back to top">
  <i class="ri-arrow-up-line"></i>
</button>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    // Newsletter form submission
    const newsletterForm = document.getElementById("newsletterForm");
    if (newsletterForm) {
      newsletterForm.addEventListener("submit", function (e) {
        e.preventDefault();
        const email = this.querySelector('input[type="email"]').value;

        // Here you would typically send the email to your backend
        console.log("Newsletter subscription:", email);

        // Show success message (you can integrate with SweetAlert2)
        if (window.Swal) {
          Swal.fire({
            icon: "success",
            title: "Subscribed!",
            text: "Thank you for subscribing to our newsletter.",
            confirmButtonColor: "#1B3C53",
          });
        } else {
          alert("Thank you for subscribing to our newsletter!");
        }

        // Reset form
        this.reset();
      });
    }

    // Scroll to top functionality
    const scrollToTopBtn = document.getElementById("scrollToTop");

    window.addEventListener("scroll", function () {
      if (window.pageYOffset > 300) {
        scrollToTopBtn.classList.add("visible");
      } else {
        scrollToTopBtn.classList.remove("visible");
      }
    });

    scrollToTopBtn.addEventListener("click", function () {
      window.scrollTo({
        top: 0,
        behavior: "smooth",
      });
    });

    // Add smooth hover effects to footer links
    const footerLinks = document.querySelectorAll(".footer-links a");
    footerLinks.forEach((link) => {
      link.addEventListener("mouseenter", function () {
        this.style.transform = "translateX(8px)";
      });

      link.addEventListener("mouseleave", function () {
        this.style.transform = "translateX(0)";
      });
    });
  });
</script>
